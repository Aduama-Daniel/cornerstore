# AI Change Log

| Date | Actor | Scope | Files/behavior changed | Evidence used | Validation | Rollback note |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-06-13 | Codex | Operating-layer audit | Added `cornerstore-os/` documentation only; no storefront or payment behavior changed | Repository code, configuration, routes, and scripts | Frontend typecheck/build and backend syntax passed; lint setup, catalog media, and Paystack dependency issues documented in `current-site-audit.md` | Delete the added documentation directory only if intentionally reverting |
| 2026-06-13 | Codex | Current Website Audit Loop | Updated `current-site-audit.md`, `conversion-issues.md`, and `backlog.md`; added severity, affected surfaces, fixes, business impact, and approval requirements. No application code changed. | All customer routes, shared UI, product, cart, checkout, account, SEO, analytics, performance, trust, and backend review/payment/admin paths | Documentation consistency checks only; application validation not run because application code was unchanged | Revert this documentation entry and the three audit document updates if intentionally discarding the audit |
| 2026-06-13 | Codex | Product Validation Loop | Rebuilt `research-log.md` with current product-addition workflows, Ghana availability signals, a 20-product scorecard, five detailed test candidates, a physical evidence checklist, and a proposed validation sprint. No products or application code changed. | Cornerstore business/product/pricing rules, catalog manifest, admin/import code, and current Ghana retailer/marketplace availability signals | Documentation consistency checks only; application validation not run because application code and catalog data were unchanged | Revert the research-log update and this entry if intentionally discarding the product screening |
| 2026-06-13 | Codex | Product Page Improvement Loop | Improved the existing product detail page with a benefit label, conservative availability messaging, configurable WhatsApp enquiry CTA, factual trust signals, key features, package-content status, delivery/payment guidance, and FAQ. Removed unsupported generic product, shipping, returns, and care claims. Added `NEXT_PUBLIC_WHATSAPP_NUMBER` to the frontend environment example. Payment and checkout behavior were not changed. | Existing product route and data shape, inventory/cart behavior, Paystack checkout implementation, business rules, and current conversion audit | `npx tsc --noEmit` passed after build; `npm run build` passed; `npm run lint` was blocked by Next.js interactive first-run ESLint setup; browser visual check was blocked by local-URL policy | Revert the product-page component changes and remove the environment example entry; no data migration or payment rollback is required |
| 2026-06-13 | Codex | Catalog population | Imported all 48 prepared catalog candidates into the connected database as inactive products. Each imported record has media, model number, specifications, category, brand, and a default inventory record. Updated the importer to use original product photos when generated presentation images are unavailable, retry transient Cloudinary failures, accept a single valid image, and support resuming from a product number. No product was publicly activated. | Catalog manifest, original product photos, generated product media, `product-rules.md`, and `pricing-rules.md` | Catalog dry run passed; database audit confirmed 48 inactive imports, 48 with media, 48 with model numbers, and 48 with specifications | Re-running the importer is idempotent. To remove the imported candidates, delete only records tagged `catalog-import` and their associated inventory/media after explicit approval |
| 2026-06-13 | Codex | Draft catalog activation | At owner request, activated all 48 imported catalog products and populated draft customer-facing defaults for short description, included item, delivery, availability, and FAQ. Marked every imported record `needs-owner-review`. Increased the shop fetch limit to 100, disabled shop-route caching during review, and changed card availability copy to “Order subject to confirmation.” | Owner approval in this thread, existing manifest descriptions/specifications, imported product names, and current Paystack/delivery flow | Public API confirmed 59 total active products, including all 48 imports; all 48 imports contain the draft fields; frontend typecheck passed | Set products tagged `catalog-import` back to `inactive` to unpublish. Review supplier, package contents, price, warranty, and current availability before paid promotion |
| 2026-06-13 | Codex | Backend logger stability | Removed the threaded `pino-pretty` development transport that crashed under Node 24 with `thread-stream: this should not happen: undefined`. Fastify now uses its built-in structured logger with configurable `LOG_LEVEL`; removed the unused `pino-pretty` dependency. | Reproduced stack trace, Fastify/Pino dependency tree, Node runtime, and server logger configuration | Backend syntax passed; `/health` and products endpoints returned 200; sustained requests produced normal logs without a worker crash | Restore the old transport and dependency only if the project moves to a verified compatible runtime/transport combination |
| 2026-06-13 | Codex | Professional catalog media replacement | Replaced product media for catalog items 001–037 with the regenerated professional renders from `images/generated/numbered`. Added an idempotent `catalog:replace-media` script that uploads to dedicated Cloudinary public IDs and changes only product media fields. Items 038–048 were unchanged because no numbered professional renders exist for them in the workspace. | Numbered catalog index, regenerated image files, visual spot checks for items 001, 012, and 037, and current database media records | Script completed 37 replacements; database/API verification confirmed professional media URLs for replaced products | Re-run the script after adding numbered files 038–048. Previous Cloudinary source images remain available but are no longer referenced by products 001–037 |
| 2026-06-13 | Claude | UI Redesign Loop — imagery + polish pass | Added visual intrigue and finished the light re-theme: homepage hero fallback + new appliance spotlight band + `/about` now use curated product/lifestyle imagery already in the repo (`public/product-listing-options/generated/*`); re-themed dark heroes on `/cart`, `/checkout`, `/wishlist`, `/collections`, `/collections/[slug]`; restyled shared `InfoPageTemplate` (faq/shipping/care/size-guide/privacy/terms/accessibility); rewrote `/about` from fashion-editorial to the Ghana convenience-store proposition; resolved duplicate font loading (next/font + CSS variables, dropped the Google Fonts `@import`). No logic, prices, claims-as-fact, or invented data added. | Existing pages/components, repo image assets, business brief | `npx tsc --noEmit` passed; `npm run build` passed; visual QA via local preview (homepage hero image + spotlight, `/about`) | Revert the listed page/component edits and restore the `@import` line in globals.css to roll back |
| 2026-06-13 | Claude | Full UI Redesign Loop | Replaced the premium-fashion editorial visual direction with a modern Ghana convenience-store system. Repointed shared Tailwind tokens + rebuilt `globals.css` component classes (brand-green CTAs, card/chip/badge/input, radii, shadows); switched headings to Plus Jakarta Sans. Rebuilt `Header` (real nav + persistent search + clean mobile menu), `Footer` (Ghana positioning, corrected links, removed London/Copenhagen/styling claims), `HeroCarousel` (light conversion hero with search + trust, keeps admin hero products), homepage (`page.tsx`: hero, category shortcuts, trending, how-ordering-works, reassurance), `ProductCard`/`ProductGrid` (modern square card, availability cue, 2-col mobile, new empty states), `search/page.tsx` (light theme, helpful no-result → assistant/request item), `Chatbot` (branded launcher + suggestions), `ProductInfo` CTAs (green add-to-cart, WhatsApp button), `ShopClient` (light header + mobile filters), and skeletons. Wrote `ui-redesign-plan.md` and `frontend-ui-audit.md`. No prices, reviews, stock, payment, search, chatbot, cart, or checkout logic changed. | Full frontend audit, existing components/routes/data shapes, business brief, current-site-audit, and conversion-issues | `npx tsc --noEmit` passed; `npm run build` passed (all routes compiled); `npm run lint` still blocked by Next.js interactive ESLint first-run setup (pre-existing, not configured); visual QA via local preview server at desktop/tablet/mobile incl. open chatbot | Revert the listed component/page/token files to restore the prior editorial theme; no data, payment, or env rollback required (only `.claude/launch.json` added for tooling) |
| 2026-06-13 | Claude | Product page rework + category wiring + polish | Fixed the product detail scroll-within-scroll by removing the buy-column internal scroll on `ProductInfo` (`lg:max-h-[78vh] lg:overflow-y-auto`), so the gallery stays sticky and the page scrolls naturally; polished the product page breadcrumb, not-found state, and related-products section. Wired real catalog categories into homepage shortcuts, footer Shop column, product breadcrumb/related via `/shop?category=<slug>` (kitchen-appliances, kettles, blenders-mixers, lighting, personal-care, home-accessories, sports-fitness, automotive-accessories); simplified the homepage product fetch off the dead fashion departments. ProductCard tag now uses brand or real category; polished the wishlist empty state. No logic, prices, payment, reviews, search, cart, or checkout behavior changed; no data invented. | Real catalog category values (`backend/src/scripts/catalog/catalog-manifest.json`), `ShopClient` category-param filtering, existing components/routes/data shapes | `npx tsc --noEmit` passed; `npm run build` passed (all routes); dev-preview QA confirmed correct fonts (Plus Jakarta Sans/Manrope resolved post clean rebuild), product page renders with zero nested scroll areas, products load (16 cards), no console errors | Revert the listed `ProductInfo`/`ProductCard`/product-page/homepage/footer edits to roll back; no data or payment rollback |
| 2026-06-13 | Claude | Featured spotlight → real products | Replaced the static single-image homepage spotlight band with a new client component `FeaturedSpotlight.tsx` that rotates through up to 5 real catalog products (real image, name, GH₵ price/sale, category, dots, auto-rotate every 5s, pauses on `prefers-reduced-motion`), each linking to its real product page. Homepage builds the list server-side from fetched products; the static lifestyle-image band remains as the no-data fallback. No prices, stock, reviews, or copy-as-fact invented. | Real catalog products from the API, existing media/currency helpers, data shapes | `npx tsc --noEmit` passed; `npm run build` compiled successfully (47 routes); dev-preview QA confirmed 5 rotating real products, auto-rotation advancing, dot selection highlighting the active product, and correct `/product/<slug>` links (product images blocked only locally because the sandbox cannot reach Cloudinary; hero local image renders) | Revert `FeaturedSpotlight.tsx` and the homepage spotlight block to restore the static image band |
| 2026-06-13 | Claude | Hero diverse-product carousel | Reworked `HeroCarousel` right-side visual from a single rotating card into a main image + 5-thumbnail rail that showcases catalogue variety: main image shows the active product with category chip + name + GH₵ price overlay; thumbnail rail highlights the active item (ring) and lets users jump; auto-rotates every 4s, pauses on hover and `prefers-reduced-motion`; "see everything →" links to `/shop`. Homepage now feeds the hero a diverse cross-category set via new `pickDiverse()` (admin `heroAdvert` products first, then one-product-per-category fill, up to 5). Lifestyle-image fallback retained when no catalogue data. No prices, stock, reviews, or copy-as-fact invented. | Real catalogue products from the API, existing media/currency helpers, admin hero-advert flag | `tsc --noEmit` passed; `npm run build` compiled successfully (40/40 pages). Live preview confirmed code path earlier via the identical FeaturedSpotlight (5 real rotating products); hero thumbnail screenshot blocked at time of change because the backend MongoDB connection dropped (`65.62.31.52:27017 closed`, infra outage) and no product data was returned — hero correctly showed the fallback | Revert the `HeroCarousel` visual block and the homepage `pickDiverse`/`heroShowcase` additions to restore the single-card hero |
| 2026-06-13 | Claude | Department mode system (Fashion vs Electronics) | Added a site-wide "department mode" that re-skins and filters the whole storefront around Fashion or Electronics. New `src/lib/modes.ts` (mode config: departments, accent colour, hero copy/image/video, categories; `filterByMode` helper), `src/contexts/ModeContext.tsx` (cookie-persisted client state + `router.refresh()` on switch), `src/lib/serverMode.ts` (SSR cookie read). `layout.tsx` wraps the app in `ModeProvider` seeded from the cookie (default Fashion). `Header` gained a Fashion/Electronics segmented toggle (desktop in the category row + always-visible full-width on mobile) and mode-aware category links. `HeroCarousel` rebuilt into a per-mode banner with accent theming, mode copy, optional looping `<video>` drop-in (`/public/hero/{mode}.mp4`, falls back to image), and a product rail. Homepage (`page.tsx`, now `force-dynamic`) filters hero/trending/spotlight/category-shortcuts by mode with accent theming and an empty state for the as-yet-untagged Fashion mode. `shop/page.tsx` filters listings to the active mode server-side; `search/page.tsx` filters results to the active mode. Mode = product `department` field; tag products with a `fashion`/`electronics` department to populate each. No prices, stock, reviews, payment, cart, or checkout logic changed; no products invented. | Owner decisions (not-tagged-yet scaffold, image-now+video-drop-in, Fashion default), product `department` schema, API department filter, existing assets | `tsc --noEmit` passed; `npm run build` compiled (40/40 pages); live preview confirmed Fashion default + Electronics switch re-skin hero/nav/categories/accent, cookie persistence (`cs_mode`), and correct empty state. Product grids could not be populated live because the backend MongoDB was down (infra) | Remove `modes.ts`, `ModeContext.tsx`, `serverMode.ts`, the `ModeProvider` wrap, the `Header` toggle, and revert `HeroCarousel`/`page.tsx`/`shop`/`search` mode filtering to roll back; delete the `cs_mode` cookie |
| 2026-06-14 | Claude | Mode UX revision — full-bleed hero, gold fashion accent, nav-style tabs | Per owner feedback: changed Fashion accent to muted gold `#C59A53` (`modes.ts`). Rebuilt `HeroCarousel` into a full-bleed, full-width auto-advancing image slideshow with overlaid copy/search/CTAs/dots (removed the side card/box); added per-mode `heroSlides` to the mode config. Copied the owner-supplied fashion images from `images/fashion-hero/` into `public/images/fashion-hero/fashion-1..6.png` and wired them as the Fashion slides; Electronics uses the existing generated lifestyle renders. Replaced the segmented mode toggle with top-of-header **nav-style department tabs** (Fashion · Electronics, accent underline on the active one, extensible to future modes like Perfumes) and merged the old green trust strip into that bar. Simplified the homepage hero data (dropped the unused hero-product showcase). No prices, stock, reviews, payment, cart, or checkout logic changed. | Owner feedback + supplied images, existing mode system | `tsc --noEmit` passed; `npm run build` compiled (40/40 pages); live preview confirmed full-bleed gold Fashion hero with the supplied slides + nav tabs, and Electronics switch (green, kettle slides) | Revert `HeroCarousel`, the `modes.ts` accent/slides, the `Header` tab bar, and the homepage hero edits; remove `public/images/fashion-hero/` to roll back |
| 2026-06-14 | Claude | Mode polish — accent tokens, carousel fix, switch loader | Per owner feedback (guided by the `design-system` skill): (1) Made the whole UI re-tint per mode by pointing the Tailwind `brand` token at CSS variables — `brand.DEFAULT = rgb(var(--accent)/<alpha>)` (channel var for opacity modifiers) + `--accent-dark/light/soft`; defaults in `globals.css`, per-mode values set on `<body>` in `layout.tsx` and instantly on switch in `ModeContext`. Result: Fashion shows zero green — header logo, search button, chatbot launcher, badges, CTAs all become gold; Electronics stays green. Added `accentRgb`/`accentDark`/`accentLight` to the mode config; fixed the one hardcoded green (chatbot "online" dot). (2) Fixed the hero carousel not advancing (removed the `prefers-reduced-motion` gate; 4.5s interval) — verified it cycles all 6 owner-supplied fashion images. (3) Added a mode-switch transition loader (dark curtain + accent mode label + animated bar) in `ModeContext`, with a hard dismiss cap (≤1.1s) so a slow/down backend can't trap the user. (4) Polished the hero (lighter image scrim, refined trust row without heavy icon circles, slimmer slide dots). Owner-supplied fashion images copied to `public/images/fashion-hero/fashion-1..6.png` (kept in public; Next/Image optimises on serve — Cloudinary not required). No prices, stock, reviews, payment, cart, or checkout logic changed. | Owner feedback + supplied images, `design-system` skill, existing token system | `tsc --noEmit` passed; `npm run build` compiled (40/40 pages); fixed a real build error (`ring-brand/25` needed channel-based `--accent`); live preview confirmed gold Fashion chrome (no green), green Electronics chrome, carousel cycling fashion slides, and Fashion↔Electronics switches landing correctly with the loader curtain. Backend MongoDB still down so product grids stayed empty (infra) | Revert the `tailwind`/`globals`/`layout`/`modes`/`ModeContext`/`HeroCarousel`/`Chatbot` edits to roll back; remove `public/images/fashion-hero/` |
| 2026-06-14 | Claude | Logo + QA pass | Replaced the navbar "C" badge + text wordmark with the owner-supplied `logo.png` (trimmed whitespace via PIL → 667×106, moved to `public/`), used in header and footer (footer inverted to white). Removed the hero slide indicator dots. QA: verified all 14 customer routes return 200, no homepage console errors, mobile menu works, empty states (cart/wishlist) and static pages render on the light theme. Fixes: rewrote `/contact` to remove dev placeholder copy and fashion-editorial slop and point to the real chat assistant + FAQ (no contact details invented — none configured); made the footer Shop column mode-aware (shows the active mode's categories); modernised the empty-cart state and fixed "Bag"→"cart" wording. Ran clean dev server on :3000 for testing. | Owner-supplied logo, running site on :3000, existing pages | `tsc --noEmit` passed; `npm run build` compiled (40/40 pages); live :3000 QA confirmed logo, removed dots, mode-aware footer, working mobile menu, 200s on all routes. Product/cart-add/checkout/search-result flows NOT testable live because the backend MongoDB is down (infra) | Revert the `Header`/`Footer`/`HeroCarousel`/`contact`/`cart` edits and restore the prior logo markup; remove `public/logo.png` |
| 2026-06-14 | Claude | Commerce-flow QA (DB live) | With the backend reconnected (59 active products), walked the full purchase flow on a local dev server and confirmed each step works: shop grid renders real products/images (48 in Electronics), product detail loads (image, price, no nested scroll), add-to-cart increments the header badge, cart shows item/quantity/total + "Proceed to Checkout", checkout form renders Ghana fields (GPS address, region/city, phone), and mode-filtered search returns results ("1 electronics result for popcorn"). Fixes from QA: cart step label "Bag"→"Cart"; `CartItem` now hides the empty "Size" chip when a product has no size and uses palette tokens instead of hardcoded `#f7f2eb`. (Did not trigger a live Paystack payment.) | Live backend, browser QA on :3000 | `tsc --noEmit` passed; `npm run build` compiled (41/41 pages); live flow verified end-to-end | Revert the `cart`/`CartItem` edits to roll back |
| 2026-06-14 | Claude | Shop filter optimisation (mode-aware, data-driven) | Reworked the shop/collection filters. Removed irrelevant facets — **Color** and **Origin** (Ghana/China) — and the generic XS–XXL **Size** list. New facets are **derived from the products actually in the active mode**, so only relevant options appear: Category + Brand (with live counts), Price (GH₵ buckets), Size (only renders when products genuinely have sizes → fashion clothing), and an In-stock toggle. Rewrote `ShopFilters` as a controlled component (fixes the desktop/mobile filter state-sync bug) styled to the design system with mode-accent checkboxes; updated `ShopClient` (facet derivation + brand filtering + a sort dropdown) and `CollectionProductsClient` to the new API; removed unused `colors`/`categories` fetches from `shop/page.tsx`; deleted dead `FilterSidebar.tsx` (stale mens/womens categories). | Product data shape (category/brand/price/status/variations), mode system | `tsc --noEmit` passed; `npm run build` compiled (41/41 pages); live verified on `/shop` (Electronics): filters show Category/Brand/Price + In-stock only (no Color/Size/Origin), real categories/brands with counts, and selecting Kettles narrows the grid to its 7 products | Revert `ShopFilters`/`ShopClient`/`CollectionProductsClient`/`shop/page.tsx` to roll back; restore `FilterSidebar.tsx` from git if needed |

## Required For Future AI Changes

- State the business objective.
- List exact files and behavior changed.
- Record assumptions and every `MISSING` input.
- Link the experiment or backlog item.
- Record validation commands and results.
- Never auto-publish products, alter prices, issue refunds, or change payment logic without an explicit approved workflow.
