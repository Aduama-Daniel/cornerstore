# Conversion Issues

Audit date: 2026-06-13

Approval values:

- `No`: implementation can proceed within existing approved behavior.
- `Yes`: requires a business, legal, tracking, security architecture, pricing, payment, or checkout decision.

## Critical

| ID | Issue | Affected file/page | Why it matters | Recommended fix | Expected business impact | Approval needed |
| --- | --- | --- | --- | --- | --- | --- |
| C-01 | Admin commerce APIs are not protected | `backend/src/routes/adminOrders.js`, `returns.js`, `analyticsAdmin.js`; `/api/admin/*` | Order/customer data and refund, manual-payment, status, and analytics actions are exposed without `adminAuth` | Apply one verified admin guard to every admin route and add unauthorized-access tests | Prevents data exposure, fraudulent mutations, and launch-ending security incidents | Yes - security/session approach |
| C-02 | Guest Paystack checkout receives an empty email | `frontend/src/app/checkout/page.tsx:193`; `/checkout` | Guest customers enter an email, but Paystack receives `user?.email || ''`; payment initialization may fail or lose customer identity | Use the validated shipping email for guests and cover guest payment in an end-to-end test | Restores guest checkout completion and reduces paid-traffic loss | Yes - checkout/payment |
| C-03 | Order totals are client-authoritative | `frontend/src/app/checkout/page.tsx:57`, `backend/src/routes/orders.js`, `backend/src/services/orderService.js` | Browser values can differ from current product, delivery, and variant prices | Recalculate subtotal, delivery, tax/fees, discounts, and total on the server | Protects margin and prevents incorrect or manipulated orders | Yes - pricing/checkout |
| C-04 | Paystack verification path is unstable | `backend/src/services/paystackService.js`, `orderService.js` | `axios` is undeclared, amount mismatch is accepted, payment states conflict, and update return handling may produce `undefined` | Stabilize dependency/import, reject amount/currency mismatch, define one payment state machine, make updates idempotent, add webhook reconciliation | Prevents paid orders from being lost, underpaid, duplicated, or excluded from revenue | Yes - payment logic |
| C-05 | Availability and inventory are not enforced end to end | `ProductInfo.tsx`, `CartContext.tsx`, `cartService.js`, `orderService.js`; product/cart/checkout | Ads can create demand for items that are unavailable; authenticated cart loses `colorSlug`; no reservation/decrement occurs | Preserve full variant identity, recheck availability at cart and order, model manual confirmation/reservation, and prevent overselling | Reduces cancellations, support load, wasted ad spend, and customer disappointment | Yes - fulfilment/checkout |
| C-06 | No usable paid-traffic funnel measurement | `ProductViewTracker.tsx`, `analyticsService.js`, `CartContext.tsx`, checkout/order flow | Product testing cannot identify which ads, products, or funnel stages work | Approve event schema and tracking/privacy approach; instrument view, cart, checkout, payment, availability, delivery, cancellation, refund, and attribution | Makes product and ad decisions measurable instead of anecdotal | Yes - analytics/privacy |
| C-07 | Product and policy claims are inaccurate or unverified | `ProductInfo.tsx:324`, `/shipping`, `/about`, footer; all product details | Appliances receive dry-cleaning, ethical-material, premium-craftsmanship, and fashion return text; shipping promises conflict | Replace generic blocks with verified category/product facts and one approved shipping/returns policy | Improves trust, reduces disputes, and avoids misleading ads | Yes - business/legal policy |
| C-08 | Reviews falsely imply purchase verification | `reviewService.js`, `ProductReviews.tsx`; product pages | Any authenticated user is auto-approved and displayed as “Verified Buyer” | Require a delivered-order/product match for the badge; moderate or clearly label unverified reviews | Protects social-proof credibility and reduces advertising/compliance risk | Yes - review policy |

## Important

| ID | Issue | Affected file/page | Why it matters | Recommended fix | Expected business impact | Approval needed |
| --- | --- | --- | --- | --- | --- | --- |
| I-01 | Mobile users cannot filter products | `ShopClient.tsx:131`, `CollectionProductsClient.tsx`; `/shop`, collections | Filters are hidden below `lg` with no drawer or controls | Add a mobile filter button and existing-filter drawer/sheet without redesigning filter logic | Faster discovery and better mobile product-page reach | No |
| I-02 | Navigation points to stale fashion collections | `Header.tsx`, `Footer.tsx`; all pages | `womens`, `mens`, `accessories`, and `new-arrivals` do not match the current appliance/lifestyle catalog | Drive navigation from active categories or approved merchandising links | Fewer dead/empty journeys and clearer category discovery | Yes - navigation/category priorities |
| I-03 | Brand positioning contradicts the Ghana marketplace model | `layout.tsx`, homepage, `HeroCarousel.tsx`, `Footer.tsx`, `/about` | Premium fashion, London, Copenhagen, worldwide, and implementation-focused copy makes the business feel incoherent | Approve one customer proposition and replace contradictory copy only | Higher ad-message match and trust | Yes - positioning |
| I-04 | Contact page has no contact method | `/contact`, `InfoPageTemplate.tsx` | Customers cannot ask availability, delivery, or order questions despite repeated prompts to contact support | Add approved email, phone, WhatsApp, support hours, or working form | Recovers high-intent shoppers and reduces uncertainty | Yes - contact operations |
| I-05 | Customer-facing links lead to missing pages | `/login`, `/account`; `/forgot-password`, `/account/addresses`, `/account/settings` | Broken links undermine trust and block account recovery | Implement the intended pages or remove links until available | Reduces abandonment and support friction | No for removal; Yes for workflow scope |
| I-06 | SEO foundation is incomplete | `layout.tsx`, all public routes | Generic fashion metadata and no sitemap, robots, canonicals, social cards, or route metadata limit discoverability and sharing | Add marketplace metadata, route/product metadata, canonicals, sitemap, robots, and index policy | Better organic discovery, ad landing-page quality, and share previews | Yes - production domain and brand copy |
| I-07 | No product structured data | `/product/[slug]`, reviews, breadcrumbs | Search engines cannot reliably interpret price, availability, brand, model, or review data | Add valid Product/Offer/Breadcrumb JSON-LD from verified server data | Eligibility for richer search presentation | No after policy/data accuracy is approved |
| I-08 | Newsletter forms are nonfunctional | `Footer.tsx`, `/about` | Submit currently reloads or does nothing, creating a false lead-capture promise | Connect an approved provider or remove/disable the forms | Prevents lost leads and misleading interaction | Yes - provider and consent text |
| I-09 | Shop inventory and scale controls are incomplete | `/shop`, `ShopFilters.tsx`, products API | In-stock filter uses status rather than inventory; no pagination; default API cap can hide products | Use inventory availability and add pagination/load-more with count | More accurate browsing and catalog coverage | No |
| I-10 | Checkout desktop grid is misconfigured | `frontend/src/app/checkout/page.tsx:152` | Form spans both grid columns, so the “visible while you move” summary drops below it | Remove the unintended two-column span and verify desktop/tablet layout | Better order review and lower checkout uncertainty | No, but checkout code should receive explicit go-ahead |
| I-11 | Guest order recovery and confirmation are weak | `/checkout`, orders API, `/account/orders` | Guests share `userId: "guest"` and have no secure order lookup; UI claims email confirmation without evidence | Add secure guest lookup token and confirmed notification workflow | Fewer “where is my order?” contacts and stronger post-purchase trust | Yes - customer support workflow |
| I-12 | Product view/add-to-cart analytics code is disconnected | `ProductViewTracker.tsx`, `analyticsService.js` | Trending products and conversion rates cannot work even before full analytics is selected | Connect existing first-party events with anonymous/session identifiers and deduplication | Immediate product-interest signal | Yes - tracking/privacy |
| I-13 | Performance loading is unnecessarily aggressive | `ProductGrid.tsx`, `ProductCard.tsx`, `globals.css`, root layout | Eight priority images, duplicate font loading, global chatbot, and repeated full catalog fetches increase mobile cost | Prioritize only true above-fold media, remove CSS font import, lazy-load chatbot, use ID-specific product fetches | Faster mobile landing and lower bounce risk | No |
| I-14 | Collections page is client-only | `/collections/page.tsx` | Main collection content waits for JavaScript and has weaker SEO/resilience | Fetch initial collection data on the server and hydrate only interactive behavior | Faster collection discovery and better indexing | No |
| I-15 | Account order details use wrong address fields and broken text encoding | `/account/orders/[id]` | Ghana checkout stores `region`/`town`, but order page reads `state`/`zipCode`/`country`; visible mojibake appears | Render current address schema with fallbacks and replace encoding artifact | Fewer customer concerns after purchase | No |
| I-16 | Internal design route is publicly exposed | `/listing-options` | Search engines or customers may see a workbench page unrelated to the store | Remove from production routing or mark `noindex` and gate it | Cleaner brand surface and index | No |
| I-17 | Product-not-found behavior is a soft 404 | `/product/[slug]` | Missing products return a normal page response rather than proper 404 semantics | Use Next.js `notFound()` and a branded not-found page | Cleaner indexing and fewer misleading URLs | No |

## Nice-To-Have

| ID | Issue | Affected file/page | Why it matters | Recommended fix | Expected business impact | Approval needed |
| --- | --- | --- | --- | --- | --- | --- |
| N-01 | Mobile product grid is one column | `ProductGrid.tsx`; listing pages | Long scrolling reduces scan speed for broad catalogs | Test a two-column compact card layout below `sm` | More products viewed per session | Yes - experiment/design |
| N-02 | Hero consumes nearly a full mobile viewport | `HeroCarousel.tsx`; homepage | Product discovery starts below a large promotional area | Test a shorter mobile hero after baseline tracking exists | Faster path to products | Yes - experiment/design |
| N-03 | Touch gallery controls are hover-oriented | `ProductImages.tsx` | Previous/next/fullscreen controls may be undiscoverable on touch | Keep controls visible on touch-sized breakpoints and add swipe later | Easier product evaluation | No |
| N-04 | Social links are generic placeholders | `Footer.tsx` | Links to platform homepages add no proof and look unfinished | Add approved Cornerstore profiles or remove links | Modest trust improvement | Yes - account details |
| N-05 | Currency copy is inconsistent | `ShopFilters.tsx` versus `formatPrice` | `GHs` and formatted `GH₵` presentation differ | Use the shared currency formatter/label convention | Cleaner local presentation | No |
| N-06 | Search does not update the URL after a new search | `/search` | Results cannot be easily shared or revisited | Push `?q=` on submit and retain server-readable query state | Better usability and measurable search terms | No |
| N-07 | Reduced-motion support is absent | `globals.css`, carousels, cards | Motion-sensitive users cannot opt out of transitions/automatic rotation | Respect `prefers-reduced-motion` and pause automatic carousel motion | Accessibility and comfort improvement | No |
| N-08 | Review images use raw `<img>` | `ProductReviews.tsx` | Misses image optimization and sizing controls | Use approved remote patterns and `next/image` | Small performance improvement | No |

## Product Page Improvement Loop - 2026-06-13

### Improved

| Related issue | Classification | Affected file/page | Change | Expected business impact | Approval needed |
| --- | --- | --- | --- | --- | --- |
| C-07 | Critical | `frontend/src/components/ProductInfo.tsx`; `/product/[slug]` | Removed unsupported ethical sourcing, premium materials, free Ghana shipping, 14-day returns, international shipping, and generic care claims. Replaced them with product data and conservative operational wording. | Reduces misleading claims, category mismatch, and trust loss on ad landing pages | No for this removal; Yes for any future shipping/returns promise |
| C-05 | Critical | `ProductInfo.tsx`; `/product/[slug]` | Replaced exact stock-count persuasion with a clear note that local availability is confirmed before fulfilment. Existing inventory still controls variant selection and add-to-cart eligibility. | Sets a more accurate expectation for locally sourced products and should reduce avoidable cancellations | No for messaging; Yes for inventory/reservation workflow changes |
| I-04 | Important | `ProductInfo.tsx`, `.env.local.example`; `/product/[slug]` | Added a configurable prefilled WhatsApp product enquiry link. When no number is configured, the CTA clearly falls back to `/contact`. | Gives uncertain high-intent visitors a direct route to ask about the exact product and selected options | Yes - approved WhatsApp number and support ownership |

### Remaining Product-Page Issues

| ID | Classification | Affected file/page | Why it matters | Recommended fix | Expected business impact | Approval needed |
| --- | --- | --- | --- | --- | --- | --- |
| PP-01 | Critical | Product data; `ProductPurchaseDetails.tsx` | Package contents are missing for current products, so the page must show a visible `MISSING` state instead of a complete in-box list | Physically inspect each test product and add verified `includedItems` to its product record | Reduces accessory disputes, returns, and pre-sale questions | Yes - product evidence |
| PP-02 | Important | Product data; `/product/[slug]` | Delivery and availability use conservative defaults because product/location-specific operating rules are not recorded | Approve fulfilment wording and populate `deliveryNote` and `availabilityNote` where a product needs special handling | Clearer buying expectations and fewer support contacts | Yes - fulfilment policy |
| PP-03 | Important | Production environment; `ProductInfo.tsx` | The real WhatsApp destination is `MISSING`; without `NEXT_PUBLIC_WHATSAPP_NUMBER`, visitors use the generic contact fallback | Set the approved international-format number and confirm who answers, hours, and response target | Converts product questions into attributable sales conversations | Yes - support operations |
| PP-04 | Important | Analytics; product CTA and checkout events | Add-to-cart and WhatsApp CTA performance are not measured in a usable funnel | Approve event names/privacy and track product view, variant selection, add-to-cart, WhatsApp click, checkout start, and paid order | Shows whether the new information and enquiry path improve conversion | Yes - analytics/privacy |
| PP-05 | Nice-to-have | Product content/data | Existing descriptions are displayed as the benefit summary because no dedicated `shortDescription` is consistently populated | Add one verified, concise benefit-focused summary per test product | Faster comprehension from mobile ad traffic | Yes - product copy approval |
